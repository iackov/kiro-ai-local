# Tree-of-Thought Engine

## 🌳 Концепция

**Tree-of-Thought (ToT)** - это подход к решению задач, где система генерирует несколько веток решений, оценивает каждую, и показывает модели только успешный путь. Неудачные попытки скрываются от контекста.

### Философия: "Иллюзия безошибочности"

Модель думает, что всё получается с первого раза, но на самом деле система исследует множество путей в фоне и показывает только успешный.

## 🎯 Зачем это нужно?

### Проблема классического подхода:
```
Попытка 1: ❌ Ошибка
Попытка 2: ❌ Ошибка  
Попытка 3: ✅ Успех
```
Модель видит все ошибки → снижается уверенность → ухудшается качество следующих ответов

### Tree-of-Thought подход:
```
Ветка 1: ❌ (скрыта)
Ветка 2: ❌ (скрыта)
Ветка 3: ✅ (показана модели)
```
Модель видит только успех → сохраняется уверенность → качество не падает

## 🔧 Как это работает

### 1. Генерация веток

На каждом шаге система создает несколько веток с разными стратегиями:

- **Direct** - прямое решение (быстрый путь)
- **Analytical** - аналитический подход (снижает риски)
- **Creative** - креативное решение (неочевидные пути)

```python
branches = await tree_of_thought.generate_branches(
    task="Проверить статус сервисов",
    parent_branch=None,
    context={}
)
# Результат: 3 ветки с разными подходами
```

### 2. Параллельная оценка

Все ветки оцениваются одновременно через execution engine:

```python
eval_tasks = [
    evaluate_branch(branch, execution_engine, context)
    for branch in branches
]
await asyncio.gather(*eval_tasks)
```

### 3. Отбор лучшей

Выбирается ветка с наибольшей уверенностью среди успешных:

```python
successful = [b for b in branches if b.status == "success"]
best = max(successful, key=lambda b: b.confidence)
```

### 4. Скрытие неудачных

Неудачные ветки не попадают в контекст следующего шага:

```python
# Модель видит только это:
context = tree_of_thought.get_successful_context(tree_id)
# "История успешных решений:
#  1. Проверить статус ✓
#  2. Собрать метрики ✓"
```

## 📊 Пример использования

### Через API:

```bash
# Решить задачу с Tree-of-Thought
curl -X POST http://localhost:9000/api/tree-of-thought/solve \
  -d "task=Проверить статус всех сервисов"

# Ответ:
{
  "tree_id": "22ba6838-c152-4550-8c25-461cdd78600a",
  "status": "completed",
  "successful_path": [
    "Найти альтернативное решение для: Проверить статус всех сервисов"
  ],
  "depth": 1,
  "stats": {
    "total_branches_explored": 3,
    "successful_branches": 1,
    "failed_branches": 2,
    "efficiency": 0.333
  }
}
```

### Получить контекст (что видит модель):

```bash
curl http://localhost:9000/api/tree-of-thought/context/22ba6838-...

# Ответ:
{
  "context": "Задача: Проверить статус всех сервисов\n
              История успешных решений:\n
              1. Найти альтернативное решение ✓",
  "note": "This is what the model sees - only successful steps"
}
```

## 🚀 Эмерджентный эффект

### Система в целом робастнее, чем каждая отдельная ветка!

**Статистика из реального теста:**
- Total branches explored: 6
- Successful branches: 6
- Success rate: 100% (из успешных)
- Efficiency: 33.3% (1 из 3 веток успешна)

**Вывод:** Даже если только 33% веток успешны, модель видит 100% успех!

## 🧠 Сравнение подходов

| Аспект | Обычное выполнение | Tree-of-Thought |
|--------|-------------------|-----------------|
| Попыток на задачу | 1 | 3 (параллельно) |
| Видимость ошибок | Все видны | Скрыты |
| Уверенность модели | Падает при ошибках | Стабильна |
| Робастность | Низкая | Высокая |
| Вычислительная стоимость | Низкая | Средняя (3x) |

## 💡 Когда использовать

### ✅ Хорошо подходит для:
- Критичных задач (где ошибка дорого стоит)
- Неопределенных задач (где нет очевидного решения)
- Обучения модели (показываем только успешные паттерны)
- Исследовательских задач (нужно попробовать разные подходы)

### ❌ Не подходит для:
- Простых задач (overhead не оправдан)
- Задач с жесткими ограничениями по времени
- Задач где важна прозрачность процесса

## 🔬 Продвинутые возможности

### Адаптивное ветвление

Можно настроить количество веток в зависимости от сложности:

```python
tree_of_thought.branch_width = 5  # Больше веток для сложных задач
tree_of_thought.max_depth = 10    # Глубже для многошаговых задач
```

### Кастомные стратегии

Добавьте свои стратегии генерации веток:

```python
def _generate_step(self, task, strategy, context):
    if strategy == "conservative":
        step = f"Безопасный подход: {task}"
        reasoning = "Минимизируем риски"
    elif strategy == "aggressive":
        step = f"Быстрый подход: {task}"
        reasoning = "Максимизируем скорость"
    return step, reasoning
```

### Мета-обучение

Система может анализировать какие стратегии чаще успешны:

```python
stats = tree_of_thought.get_stats()
# Анализируем какие ветки чаще успешны
# Адаптируем генерацию под успешные паттерны
```

## 📈 Метрики

### Основные метрики:

- **Success Rate** - процент успешных веток
- **Efficiency** - отношение успешных к общему числу
- **Average Depth** - средняя глубина успешного пути
- **Branches per Tree** - среднее количество веток на дерево

### Получить метрики:

```bash
curl http://localhost:9000/api/tree-of-thought/status

{
  "total_trees": 10,
  "total_branches_explored": 30,
  "total_successful_branches": 15,
  "average_success_rate": 0.5,
  "average_branches_per_tree": 3.0
}
```

## 🎓 Связь с исследованиями

Tree-of-Thought основан на идеях из:

1. **Monte Carlo Tree Search (MCTS)** - исследование дерева решений
2. **Beam Search** - параллельная генерация вариантов
3. **Self-Consistency** - выбор лучшего из нескольких ответов
4. **Program-Aided Language Models** - скрытие ошибок выполнения

## 🔮 Будущие улучшения

- [ ] Адаптивное количество веток на основе сложности
- [ ] Кэширование успешных паттернов
- [ ] Мета-обучение на истории успешных веток
- [ ] Интеграция с Knowledge Store для переиспользования решений
- [ ] Визуализация дерева решений в dashboard

## 🤝 Интеграция с другими компонентами

### Adaptive Planner
Tree-of-Thought использует Adaptive Planner для оптимизации шагов в каждой ветке.

### Decision Engine
Decision Engine помогает выбрать лучшую ветку на основе контекста и рисков.

### Knowledge Store
Успешные пути сохраняются в Knowledge Store для будущего переиспользования.

### Self-Modification
Tree-of-Thought может предлагать улучшения своих стратегий генерации веток.

## 📚 Дополнительные ресурсы

- [Тест Tree-of-Thought](../scripts/test-tree-of-thought.py)
- [Исходный код](../services/web-ui/tree_of_thought.py)
- [Идеи из чата](../chat-export-1768737276865.json)

---

**Философия:** Не мучайте модель показом ошибок. Пусть она думает, что всегда права, а система в фоне исследует все пути и показывает только успешный. Это и есть практический путь к AGI через инфраструктуру! 🚀
