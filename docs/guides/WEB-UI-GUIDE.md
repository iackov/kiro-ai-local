# 🌐 AI Combiner Stack - Web UI Guide

**URL:** http://localhost:9000  
**Status:** ✅ Running  
**Container:** ai-web-ui

---

## 🚀 Quick Start

### 1. Open Web Interface

```
http://localhost:9000
```

Откроется панель управления с 6 основными разделами.

---

## 📊 Разделы интерфейса

### 1. System Status (Статус системы)

**Что показывает:**
- Статус всех сервисов (RAG API, Arch Engine, Ollama)
- Порты каждого сервиса
- Цветовая индикация (зеленый = работает, красный = не работает)

**Обновление:** Автоматически каждые 10 секунд

**Пример:**
```
✓ RAG API      :9001  [healthy]
✓ Arch Engine  :9004  [healthy]
✓ Ollama       :11434 [healthy]
```

---

### 2. RAG Query (Поиск в базе знаний)

**Что делает:**
Ищет документы в вашей базе из 19K+ документов

**Как использовать:**

1. **Введите запрос:**
   ```
   Docker troubleshooting
   ```

2. **Выберите количество результатов:**
   ```
   Top K Results: 5
   ```

3. **Нажмите "Search"**

**Результат:**
- Количество найденных документов
- Время поиска (обычно 150-400ms)
- Список документов с оценкой релевантности
- Превью содержимого каждого документа

**Примеры запросов:**
- "Python Flask examples"
- "Docker Compose configuration"
- "Redis cache setup"
- "Nginx reverse proxy"
- "MongoDB backup"

---

### 3. Architecture Engine (Модификация архитектуры)

**Что делает:**
Изменяет docker-compose.yml через естественный язык

**Как использовать:**

#### Шаг 1: Предложить изменение

Введите команду на естественном языке:

**Примеры команд:**

**Добавить сервис:**
```
Add Postgres database service
```

**Изменить ресурсы:**
```
Change Ollama memory to 16G
```

**Изменить порт:**
```
Change Redis port to 6380
```

**Удалить сервис:**
```
Remove MongoDB service
```

#### Шаг 2: Проверить предложение

После нажатия "Propose Change" вы увидите:

1. **Change ID** - уникальный идентификатор изменения
2. **Intent** - что система поняла (add service, modify resource, etc.)
3. **Diff** - что именно изменится в docker-compose.yml
4. **Safety Checks** - 5 проверок безопасности:
   - ✓ no_privileged - контейнер не привилегированный
   - ✓ resource_limits - лимиты ресурсов установлены
   - ✓ network_isolation - сеть изолирована
   - ✓ volume_safety - нет опасных монтирований
   - ✓ no_host_network - не использует host network

#### Шаг 3: Применить изменение

Если все проверки прошли, появится кнопка **"Apply Change"**

После применения:
1. Получите **Rollback ID** для отката
2. Следуйте инструкциям:
   ```
   docker compose down
   docker compose up -d
   ```

**⚠️ Важно:** После применения изменений нужно перезапустить стек!

---

### 4. Qwen Generation (Генерация текста)

**Что делает:**
Генерирует текст с помощью Qwen2.5-Coder 7B

**Как использовать:**

1. **Введите промпт:**
   ```
   Explain how Docker Compose works
   ```

2. **Выберите модель:**
   ```
   Qwen2.5-Coder 7B (по умолчанию)
   ```

3. **Нажмите "Generate"**

**Результат:**
Сгенерированный текст от Qwen

**Примеры промптов:**

**Объяснение:**
```
Explain the difference between Docker and Docker Compose
```

**Генерация кода:**
```
Create a Python Flask API with Redis cache
```

**Отладка:**
```
How to debug Docker container networking issues?
```

**Оптимизация:**
```
Suggest optimizations for a Flask + Redis production setup
```

---

### 5. RAG Statistics (Статистика базы знаний)

**Что показывает:**
- Общее количество документов
- Название коллекции

**Как использовать:**
1. Нажмите "Refresh Stats"
2. Увидите актуальную статистику

**Пример:**
```
Documents: 19,103
Collection: rag_documents
```

---

### 6. Architecture History (История изменений)

**Что показывает:**
История всех изменений архитектуры через Arch Engine

**Как использовать:**
1. Нажмите "Refresh History"
2. Увидите список изменений

**Каждое изменение содержит:**
- **SHA** - Git commit hash
- **Message** - описание изменения
- **Timestamp** - дата и время

**Пример:**
```
[77eda91b] Change redis memory to 2G
2026-01-16 08:59:49

[8f62c89c] Add Redis cache service
2026-01-16 08:57:13
```

---

## 🎯 Типичные сценарии использования

### Сценарий 1: Поиск информации + Генерация кода

**Задача:** Найти примеры Docker и создать новый docker-compose

**Шаги:**

1. **RAG Query:**
   ```
   Query: Docker Python Flask
   Top K: 5
   → Search
   ```

2. **Изучить результаты** (найдено 5 документов за 388ms)

3. **Qwen Generation:**
   ```
   Prompt: Based on Docker and Flask best practices, create a production-ready docker-compose.yml with Flask app, Redis cache, and Nginx proxy
   → Generate
   ```

4. **Получить готовый код** от Qwen

---

### Сценарий 2: Добавить новый сервис

**Задача:** Добавить PostgreSQL в стек

**Шаги:**

1. **Architecture Engine:**
   ```
   Command: Add Postgres database service
   → Propose Change
   ```

2. **Проверить предложение:**
   - Change ID: abc123def456
   - Intent: add service (postgres)
   - Diff: показывает новый сервис
   - Safety: 5/5 checks passed ✓

3. **Применить:**
   ```
   → Apply Change
   ```

4. **Перезапустить стек:**
   ```powershell
   docker compose down
   docker compose up -d
   ```

5. **Проверить:**
   ```
   System Status → Postgres [healthy]
   ```

---

### Сценарий 3: Оптимизация ресурсов

**Задача:** Увеличить память для Ollama

**Шаги:**

1. **Architecture Engine:**
   ```
   Command: Change Ollama memory to 16G
   → Propose Change
   ```

2. **Проверить diff:**
   ```diff
   - ollama.deploy.resources.limits.memory: 8G
   + ollama.deploy.resources.limits.memory: 16G
   ```

3. **Применить:**
   ```
   → Apply Change
   ```

4. **Перезапустить:**
   ```powershell
   docker compose down
   docker compose up -d
   ```

---

### Сценарий 4: Исследование базы знаний

**Задача:** Найти все заметки про Redis

**Шаги:**

1. **RAG Query:**
   ```
   Query: Redis cache configuration
   Top K: 10
   → Search
   ```

2. **Изучить результаты:**
   - Score: 0.798 (высокая релевантность)
   - Content: превью документа
   - Source: путь к файлу

3. **Уточнить поиск:**
   ```
   Query: Redis production setup best practices
   Top K: 5
   → Search
   ```

---

## 🔧 Управление через Web UI

### Запуск/Остановка

**Запустить Web UI:**
```powershell
docker compose up -d web-ui
```

**Остановить Web UI:**
```powershell
docker compose stop web-ui
```

**Перезапустить Web UI:**
```powershell
docker compose restart web-ui
```

**Проверить логи:**
```powershell
docker compose logs web-ui --tail 50
```

---

## 🎨 Интерфейс

### Цветовая схема

- **Фиолетовый градиент** - основной фон
- **Белые карточки** - разделы функционала
- **Зеленый** - успешные операции, healthy статус
- **Красный** - ошибки, unhealthy статус
- **Желтый** - предупреждения, unknown статус
- **Синий** - акценты, кнопки

### Адаптивность

- Автоматическая сетка (grid)
- Минимальная ширина карточки: 400px
- Работает на десктопе и планшете

---

## 📊 Мониторинг

### Автоматическое обновление

**System Status** обновляется каждые 10 секунд автоматически

### Ручное обновление

- **RAG Statistics** - кнопка "Refresh Stats"
- **Architecture History** - кнопка "Refresh History"

---

## 🚨 Troubleshooting

### Web UI не открывается

**Проблема:** http://localhost:9000 не отвечает

**Решение:**
```powershell
# Проверить статус
docker compose ps web-ui

# Проверить логи
docker compose logs web-ui

# Перезапустить
docker compose restart web-ui
```

### Сервисы показывают "unhealthy"

**Проблема:** RAG API или Arch Engine не отвечают

**Решение:**
```powershell
# Проверить все сервисы
docker compose ps

# Перезапустить проблемный сервис
docker compose restart rag-api
docker compose restart arch-engine
```

### RAG Query не работает

**Проблема:** Поиск возвращает ошибку

**Решение:**
```powershell
# Проверить RAG API
curl http://localhost:9001/health

# Проверить ChromaDB
curl http://localhost:9001/inspect

# Перезапустить RAG API
docker compose restart rag-api
```

### Architecture Engine не применяет изменения

**Проблема:** Apply Change возвращает ошибку 500

**Решение:**
```powershell
# Проверить логи
docker compose logs arch-engine --tail 50

# Проверить Git
git status

# Перезапустить
docker compose restart arch-engine
```

---

## 🎯 Лучшие практики

### 1. Безопасность изменений

✅ **Всегда проверяйте diff** перед применением  
✅ **Читайте safety checks** - все должны быть зелеными  
✅ **Сохраняйте Rollback ID** на случай отката  
✅ **Делайте backup** docker-compose.yml перед большими изменениями

### 2. Эффективный поиск

✅ **Используйте конкретные термины** вместо общих  
✅ **Начинайте с top_k=5**, увеличивайте при необходимости  
✅ **Комбинируйте ключевые слова** ("Docker Redis production")  
✅ **Проверяйте score** - выше 0.7 = хорошая релевантность

### 3. Работа с Qwen

✅ **Будьте конкретны** в промптах  
✅ **Указывайте контекст** ("production setup", "for beginners")  
✅ **Используйте RAG результаты** как контекст для Qwen  
✅ **Ждите 5-10 секунд** на генерацию

### 4. Мониторинг

✅ **Проверяйте System Status** перед операциями  
✅ **Обновляйте статистику** после импорта документов  
✅ **Следите за Architecture History** для аудита изменений  
✅ **Проверяйте логи** при проблемах

---

## 📱 Горячие клавиши

В браузере:
- **Ctrl+R** - обновить страницу
- **F5** - обновить страницу
- **Ctrl+Shift+I** - открыть DevTools (для отладки)

---

## 🎉 Готово!

**Web UI полностью функционален и готов к использованию!**

**Основные возможности:**
- ✅ Мониторинг всех сервисов
- ✅ Поиск в 19K+ документах
- ✅ Модификация архитектуры через NL
- ✅ Генерация текста с Qwen
- ✅ Статистика и история
- ✅ Автообновление статуса

**Откройте:** http://localhost:9000

**Начните с:**
1. Проверьте System Status (все зеленые?)
2. Попробуйте RAG Query ("Docker examples")
3. Сгенерируйте текст с Qwen
4. Предложите изменение архитектуры

**Приятного использования! 🚀**
