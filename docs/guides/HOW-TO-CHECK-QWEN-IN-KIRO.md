# Как проверить что Qwen MCP работает в Kiro IDE

## Шаг 1: Откройте MCP Panel

1. В Kiro IDE найдите панель **MCP** (обычно слева или справа)
2. Или используйте Command Palette: `Ctrl+Shift+P` → "MCP"

## Шаг 2: Проверьте статус qwen-chat

В MCP Panel должен быть сервер **qwen-chat**:

### ✅ Если работает:
```
qwen-chat
  Status: Connected ✓
  Tools: 2 available
    - qwen_chat
    - qwen_new_chat
```

### ❌ Если не работает:
```
qwen-chat
  Status: Disconnected ✗
  Error: [описание ошибки]
```

## Шаг 3: Попробуйте переподключить

Если статус "Disconnected":

1. В MCP Panel найдите сервер `qwen-chat`
2. Нажмите кнопку **Reconnect** (🔄)
3. Подождите 5-10 секунд
4. Проверьте статус снова

## Шаг 4: Тестовый запрос

В чате Kiro напишите:

```
@kiro используй qwen_chat чтобы сказать "Hello from Kiro!"
```

### ✅ Если работает:
Вы получите ответ от Qwen (обычно на английском или китайском)

### ❌ Если не работает:
Вы увидите ошибку типа:
- "Tool qwen_chat not found"
- "MCP server not connected"
- "Token has expired"

## Шаг 5: Проверка логов (если не работает)

### В Kiro IDE:
1. Откройте **Output Panel** (View → Output)
2. Выберите канал **MCP** или **qwen-chat**
3. Посмотрите последние ошибки

### Типичные ошибки:

#### Ошибка 1: "Token has expired"
**Решение**: Обновите куки
```powershell
.\scripts\refresh-qwen-cookies.ps1
```

#### Ошибка 2: "Python not found"
**Решение**: Проверьте что Python в PATH
```powershell
python --version
```

#### Ошибка 3: "Module not found"
**Решение**: Проверьте путь в MCP конфиге
```powershell
notepad "$env:USERPROFILE\.kiro\settings\mcp.json"
```

Путь должен быть:
```json
{
  "mcpServers": {
    "qwen-chat": {
      "command": "python",
      "args": ["C:\\Users\\Jack\\source\\kiro\\qwen\\src\\mcp_server\\qwen_mcp_server.py"],
      "disabled": false
    }
  }
}
```

## Шаг 6: Полный перезапуск (если ничего не помогло)

1. Закройте Kiro IDE полностью
2. Проверьте что MongoDB запущена:
   ```powershell
   docker ps | findstr mongodb
   ```
3. Запустите Kiro IDE снова
4. Подождите 10-15 секунд (MCP серверы подключаются)
5. Проверьте MCP Panel

## Быстрая диагностика из PowerShell

Запустите этот скрипт для полной проверки:

```powershell
.\scripts\test-qwen-mcp.ps1
```

Если все тесты прошли (✅), но в Kiro не работает:
- Проблема в самой Kiro IDE
- Попробуйте перезапустить Kiro
- Проверьте Output Panel для деталей

## Альтернативный тест (без Kiro)

Можно протестировать Qwen MCP напрямую:

```powershell
# Запустить MCP сервер вручную
cd C:\Users\Jack\source\kiro\qwen\src\mcp_server
python qwen_mcp_server.py
```

Сервер должен запуститься и ждать ввода (stdin).
Если запустился без ошибок - значит работает.

Нажмите `Ctrl+C` чтобы остановить.

## Что делать если работает в тестах, но не в Kiro?

1. **Перезапустите Kiro IDE** - самое простое решение
2. **Проверьте версию Kiro** - MCP может не поддерживаться в старых версиях
3. **Проверьте другие MCP серверы** - если они тоже не работают, проблема в Kiro MCP
4. **Посмотрите Kiro логи** - обычно в `~/.kiro/logs/`

## Успешный результат

Когда всё работает, вы увидите:

```
User: @kiro используй qwen_chat чтобы объяснить что такое MCP

Kiro: [Вызывает qwen_chat...]

Qwen: MCP (Model Context Protocol) is a protocol that allows 
AI assistants to interact with external tools and services...
[полный ответ от Qwen]
```

И в MongoDB сохранится история этого чата!

## Нужна помощь?

Если ничего не помогло:
1. Скопируйте ошибку из Output Panel
2. Запустите `.\scripts\test-qwen-mcp.ps1` и покажите результат
3. Проверьте что MongoDB запущена: `docker ps`
