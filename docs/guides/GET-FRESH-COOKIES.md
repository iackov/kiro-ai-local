# Как получить свежие куки Qwen

## Проблема
```
Token has expired, please log in again.
```

Это значит что куки протухли и нужны новые.

## Решение (пошагово)

### 1. Откройте браузер (Chrome/Edge)

### 2. Перейдите на https://chat.qwen.ai

### 3. Войдите в аккаунт
- Если уже вошли - выйдите и войдите заново
- Это обновит токен

### 4. Откройте DevTools
- Нажмите **F12**
- Или правой кнопкой → "Inspect" → вкладка **Network**

### 5. Отправьте сообщение в чат
- Напишите любое сообщение (например "привет")
- Отправьте его

### 6. Найдите запрос
В Network tab найдите запрос:
```
completions
```
или полный путь:
```
/api/v2/chat/completions
```

### 7. Скопируйте Cookie
- Кликните на этот запрос
- Перейдите на вкладку **Headers**
- Прокрутите вниз до **Request Headers**
- Найдите строку **Cookie:**
- Скопируйте **ВСЁ** значение после "Cookie: " (может быть очень длинным!)

### 8. Обновите конфиг

**Вариант А - через скрипт (рекомендуется):**
```powershell
.\scripts\refresh-qwen-cookies.ps1
```
Вставьте скопированную cookie строку когда попросит.

**Вариант Б - вручную:**
```powershell
notepad "C:\Users\Jack\source\kiro\qwen\src\mcp_server\qwen_config.json"
```

Найдите:
```json
{
  "cookies": {
    "cookie_string": "СТАРЫЕ_КУКИ_ЗДЕСЬ"
  }
}
```

Замените на:
```json
{
  "cookies": {
    "cookie_string": "ВСТАВЬТЕ_НОВЫЕ_КУКИ_СЮДА"
  }
}
```

**ВАЖНО**: Вставьте ВСЮ строку целиком, она может быть 2000-3000 символов!

### 9. Перезапустите MCP в Kiro

**Вариант А - через UI:**
- Откройте панель MCP в Kiro
- Найдите сервер "qwen-chat"
- Нажмите кнопку переподключения (🔄)

**Вариант Б - перезапуск Kiro:**
- Закройте Kiro IDE
- Откройте снова

### 10. Проверка

В Kiro напишите:
```
@kiro используй qwen_chat чтобы сказать привет
```

Если работает - увидите ответ от Qwen!

## Частые ошибки

### ❌ Скопировал не всю строку
Cookie должна быть 2000+ символов. Если меньше - скопировали не полностью.

### ❌ Скопировал из старого запроса
Убедитесь что запрос свежий (только что отправили сообщение).

### ❌ Не вышел/вошел заново
Если долго были в системе - выйдите и войдите для обновления токена.

### ❌ Забыл перезапустить MCP
После изменения конфига обязательно переподключите MCP сервер.

## Как часто обновлять?

Куки Qwen обычно живут **несколько дней**. Когда увидите ошибку "Token has expired" - повторите процесс.

## Автоматизация (будущее)

В будущем можно настроить автоматическое обновление токенов, но пока это ручной процесс.
