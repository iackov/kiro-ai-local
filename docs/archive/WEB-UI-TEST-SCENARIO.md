# 🎯 Web UI - Test Scenario

**Цель:** Проверить все функции веб-интерфейса  
**Время:** ~10 минут  
**URL:** http://localhost:9000

---

## 📋 Сценарий тестирования

### Шаг 1: Открыть интерфейс (30 сек)

**Действие:**
1. Откройте браузер (Chrome, Firefox, Edge)
2. Перейдите на http://localhost:9000

**Ожидаемый результат:**
- ✅ Страница загрузилась
- ✅ Видно фиолетовый градиент
- ✅ Заголовок "AI Combiner Stack - Control Panel"
- ✅ Раздел "System Status" вверху
- ✅ 5 карточек ниже (RAG Query, Architecture Engine, Qwen, Statistics, History)

**Если не работает:**
```powershell
# Проверить статус
docker compose ps web-ui

# Проверить логи
docker compose logs web-ui --tail 20
```

---

### Шаг 2: Проверить System Status (1 мин)

**Действие:**
1. Посмотрите на раздел "System Status"
2. Подождите 10 секунд (должен обновиться автоматически)

**Ожидаемый результат:**
- ✅ Видно 3 сервиса:
  - RAG API :9001 [healthy]
  - Arch Engine :9004 [healthy]
  - Ollama :11434 [healthy]
- ✅ Все бэджи зеленые
- ✅ Статус обновился через 10 секунд

**Если unhealthy:**
```powershell
# Проверить проблемный сервис
docker compose ps
docker compose logs rag-api --tail 20
docker compose logs arch-engine --tail 20
docker compose logs ollama --tail 20
```

---

### Шаг 3: Тест RAG Query (2 мин)

**Действие:**
1. Найдите карточку "RAG Query"
2. В поле "Search Query" введите:
   ```
   Docker Redis
   ```
3. В поле "Top K Results" оставьте: `5`
4. Нажмите кнопку **"Search"**
5. Подождите 1-2 секунды

**Ожидаемый результат:**
- ✅ Появился блок с результатами
- ✅ Видно: "Found X results in XXXms"
- ✅ Показано 3-5 документов
- ✅ У каждого документа:
  - Синий бэдж "Score: 0.XXX"
  - Превью текста (первые 200 символов)
- ✅ Время поиска < 500ms

**Пример результата:**
```
Found 3 results in 388.84ms

[Score: 0.798] Optimizing Docker Images with Alpine Linux...
[Score: 0.752] Docker Compose configuration for Redis...
[Score: 0.701] Redis cache setup in production...
```

**Попробуйте другие запросы:**
- "Python Flask"
- "MongoDB backup"
- "Nginx configuration"

---

### Шаг 4: Тест Architecture Engine (3 мин)

**Действие 1: Propose Change**
1. Найдите карточку "Architecture Engine"
2. В текстовом поле введите:
   ```
   Add Postgres database service
   ```
3. Нажмите **"Propose Change"**
4. Подождите 1-2 секунды

**Ожидаемый результат:**
- ✅ Появился блок с результатами
- ✅ Видно:
  - Change ID: abc123def456
  - Intent: add service
  - Diff: показывает новый сервис postgres
  - Safety Checks: 5 зеленых галочек
    - ✓ no_privileged: OK
    - ✓ resource_limits: OK
    - ✓ network_isolation: OK
    - ✓ volume_safety: OK
    - ✓ no_host_network: OK
  - Зеленая кнопка **"Apply Change"**

**Действие 2: Apply Change (ОПЦИОНАЛЬНО)**

⚠️ **ВНИМАНИЕ:** Это реально изменит docker-compose.yml!

Если хотите применить:
1. Нажмите **"Apply Change"**
2. Подождите 1-2 секунды

**Ожидаемый результат:**
- ✅ Сообщение: "Change Applied Successfully!"
- ✅ Rollback ID: 20260116_XXXXXX_abc123
- ✅ Next Steps:
  - Run: docker compose down
  - Run: docker compose up -d
  - Verify: docker compose ps

**Если применили, откатите:**
```powershell
# Откатить изменения
git checkout docker-compose.yml

# Или использовать rollback через API
curl -X POST http://localhost:9004/arch/rollback `
  -H "Content-Type: application/json" `
  -d '{"rollback_id": "20260116_XXXXXX_abc123"}'
```

**Попробуйте другие команды (без Apply):**
- "Change Redis memory to 4G"
- "Change Ollama port to 11435"
- "Add volume /data/cache to Redis"

---

### Шаг 5: Тест Qwen Generation (2 мин)

**Действие:**
1. Найдите карточку "Qwen Generation"
2. В текстовом поле введите:
   ```
   Explain what Docker Compose is in 2 sentences
   ```
3. Модель оставьте: `Qwen2.5-Coder 7B`
4. Нажмите **"Generate"**
5. Подождите 5-10 секунд (идет генерация)

**Ожидаемый результат:**
- ✅ Появился блок "Generating..."
- ✅ Через 5-10 секунд появился текст от Qwen
- ✅ Текст осмысленный и на тему
- ✅ Примерно 2-3 предложения

**Пример ответа:**
```
Docker Compose is a tool for defining and running multi-container 
Docker applications using a YAML file. It allows you to configure 
your application's services, networks, and volumes in a single file, 
making it easy to start, stop, and manage complex applications with 
a single command.
```

**Попробуйте другие промпты:**
- "What is the difference between Docker and Kubernetes?"
- "How to optimize Docker images?"
- "Create a simple Flask API example"

---

### Шаг 6: Тест RAG Statistics (30 сек)

**Действие:**
1. Найдите карточку "RAG Statistics"
2. Нажмите **"Refresh Stats"**
3. Подождите 1 секунду

**Ожидаемый результат:**
- ✅ Появились 2 блока:
  - Documents: 19103 (или около того)
  - Collection: rag_documents
- ✅ Цифры синие и крупные
- ✅ Подписи серые и мелкие

**Если показывает 0 документов:**
```powershell
# Проверить RAG API
curl http://localhost:9001/inspect

# Проверить ChromaDB
docker compose logs rag-api | grep -i chroma
```

---

### Шаг 7: Тест Architecture History (30 сек)

**Действие:**
1. Найдите карточку "Architecture History"
2. Нажмите **"Refresh History"**
3. Подождите 1 секунду

**Ожидаемый результат:**
- ✅ Появился список изменений (2-3 записи)
- ✅ Каждая запись содержит:
  - SHA: [77eda91b]
  - Message: "Change redis memory to 2G"
  - Timestamp: 2026-01-16 08:59:49
- ✅ Записи отсортированы по времени (новые сверху)

**Если пусто:**
```powershell
# Проверить Git историю
git log --oneline --grep="arch-engine"

# Проверить Arch Engine
curl http://localhost:9004/arch/history
```

---

## ✅ Чек-лист проверки

Отметьте что работает:

- [ ] Страница открылась
- [ ] System Status показывает 3 сервиса
- [ ] Все сервисы [healthy]
- [ ] RAG Query нашел документы
- [ ] Время поиска < 500ms
- [ ] Architecture Engine распознал команду
- [ ] Показал diff и safety checks
- [ ] Qwen сгенерировал текст
- [ ] Текст осмысленный
- [ ] RAG Statistics показал количество документов
- [ ] Architecture History показал изменения
- [ ] Интерфейс красивый и понятный
- [ ] Нет ошибок в консоли браузера (F12)

---

## 🎯 Продвинутый тест (опционально)

### Тест 1: Комбинированный сценарий

**Задача:** Найти информацию и использовать для генерации

1. **RAG Query:**
   ```
   Query: Docker production best practices
   Top K: 3
   → Search
   ```

2. **Скопировать результат** (первый документ)

3. **Qwen Generation:**
   ```
   Prompt: Based on this context: [вставить текст из RAG]
   
   Create a production-ready docker-compose.yml for a Python Flask app
   → Generate
   ```

4. **Проверить:** Qwen использовал контекст из RAG

---

### Тест 2: Модификация архитектуры

**Задача:** Добавить сервис и проверить

1. **Architecture Engine:**
   ```
   Command: Add Nginx proxy service
   → Propose Change
   ```

2. **Проверить diff:**
   - Должен показать новый сервис nginx
   - Порт 80:80
   - Image: nginx:alpine

3. **НЕ применять** (или применить и откатить)

---

### Тест 3: Мониторинг в реальном времени

**Задача:** Проверить авто-обновление

1. Откройте Web UI
2. Откройте консоль браузера (F12)
3. Перейдите на вкладку Network
4. Подождите 10 секунд
5. Увидите запрос к `/api/status`
6. Еще через 10 секунд - еще один запрос

**Ожидаемый результат:**
- ✅ Запросы идут каждые 10 секунд
- ✅ Статус обновляется автоматически

---

## 🐛 Troubleshooting

### Проблема 1: Страница не открывается

**Симптом:** ERR_CONNECTION_REFUSED

**Решение:**
```powershell
# Проверить контейнер
docker compose ps web-ui

# Если не запущен
docker compose up -d web-ui

# Проверить логи
docker compose logs web-ui
```

---

### Проблема 2: RAG Query возвращает ошибку

**Симптом:** "Error: ..."

**Решение:**
```powershell
# Проверить RAG API
curl http://localhost:9001/health

# Перезапустить
docker compose restart rag-api

# Подождать 30 секунд (загрузка модели)
```

---

### Проблема 3: Architecture Engine не работает

**Симптом:** "Error: 500 Internal Server Error"

**Решение:**
```powershell
# Проверить логи
docker compose logs arch-engine --tail 50

# Проверить Git
git status

# Перезапустить
docker compose restart arch-engine
```

---

### Проблема 4: Qwen не генерирует

**Симптом:** Долго висит "Generating..." или ошибка

**Решение:**
```powershell
# Проверить Ollama
curl http://localhost:11434/api/tags

# Проверить модель
docker exec ai-ollama ollama list

# Если модели нет
docker exec ai-ollama ollama pull qwen2.5-coder:7b
```

---

## 📊 Ожидаемые результаты

### Производительность

| Операция | Время | Статус |
|----------|-------|--------|
| Загрузка страницы | <500ms | ✅ |
| System Status | <200ms | ✅ |
| RAG Query | 150-400ms | ✅ |
| Arch Propose | <500ms | ✅ |
| Arch Apply | <2s | ✅ |
| Qwen Generate | 5-10s | ✅ |
| Statistics | <200ms | ✅ |
| History | <200ms | ✅ |

### Функциональность

| Функция | Работает | Примечание |
|---------|----------|------------|
| Мониторинг | ✅ | Авто-обновление 10s |
| RAG поиск | ✅ | 19K+ документов |
| Arch модификация | ✅ | NL → Docker |
| Qwen генерация | ✅ | 7B модель |
| Статистика | ✅ | Real-time |
| История | ✅ | Git commits |

---

## 🎉 Результат теста

После прохождения всех шагов вы должны увидеть:

✅ **Web UI полностью функционален**
- Все 6 разделов работают
- Нет ошибок
- Интерфейс отзывчивый
- Данные актуальные

✅ **Интеграция работает**
- RAG API отвечает
- Arch Engine модифицирует
- Qwen генерирует
- Git сохраняет историю

✅ **UX приятный**
- Красивый дизайн
- Понятные кнопки
- Быстрая обратная связь
- Информативные сообщения

---

## 📝 Отчет о тесте

После тестирования заполните:

**Дата:** ___________  
**Время:** ___________  
**Браузер:** ___________

**Что работает:**
- [ ] System Status
- [ ] RAG Query
- [ ] Architecture Engine
- [ ] Qwen Generation
- [ ] RAG Statistics
- [ ] Architecture History

**Найденные проблемы:**
1. ___________
2. ___________
3. ___________

**Общая оценка:** ___/10

**Комментарии:**
___________________________________________
___________________________________________

---

## 🚀 Следующие шаги

После успешного теста:

1. **Используйте регулярно** - добавьте в закладки
2. **Экспериментируйте** - пробуйте разные команды
3. **Документируйте** - записывайте полезные запросы
4. **Делитесь** - покажите коллегам

**Web UI готов к продуктивной работе!** 🎊
