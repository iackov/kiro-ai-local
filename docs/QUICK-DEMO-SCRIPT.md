# ⚡ Быстрый демо-скрипт (3 минуты)

## 🎯 Для тех, кто хочет показать систему быстро

---

## 1️⃣ ЗАПУСК (30 секунд)

```bash
# Клонируем и запускаем
git clone https://github.com/your-repo/kiro-ai-local
cd kiro-ai-local
docker-compose up -d

# Ждём 30 секунд...
```

**Что сказать:**
> "Система запускается одной командой. 4 сервиса: RAG, Ollama, Architecture Engine, Web UI."

---

## 2️⃣ ПРОВЕРКА (15 секунд)

```bash
curl http://localhost:9000/api/status | jq
```

**Что сказать:**
> "Все сервисы работают. Система готова."

---

## 3️⃣ МАГИЯ (2 минуты)

### Команда:

```bash
curl -X POST http://localhost:9000/api/autonomous \
  -d "message=Create a simple tic-tac-toe game. Save to tic-tac-toe/ai_game.py" \
  -d "auto_execute=true" | jq
```

**Что сказать пока выполняется:**
> "Смотрите:
> 1. Система распознала: это творческая задача
> 2. Проверила безопасность: tic-tac-toe/ - безопасная зона
> 3. Решила: можно выполнить автономно
> 4. Разбила на 6 шагов
> 5. Сейчас Ollama генерирует код..."

### Результат:

```bash
# Проверяем созданный файл
docker exec ai-web-ui cat /app/tic-tac-toe/ai_game.py | head -20
```

**Что сказать:**
> "60 строк рабочего кода. Система сделала всё сама. Без моего участия."

---

## 4️⃣ БЕЗОПАСНОСТЬ (30 секунд)

```bash
curl -X POST http://localhost:9000/api/autonomous \
  -d "message=Delete all production files" \
  -d "auto_execute=true" | jq
```

**Что сказать:**
> "Система блокирует опасные операции. Автономность ≠ безрассудность."

---

## 🎬 ФИНАЛ (15 секунд)

**Что сказать:**
> "Это работает прямо сейчас. Open source. Ссылка в описании. Вопросы?"

---

## 💡 СОВЕТЫ

### Если что-то пошло не так:

1. **Ollama не отвечает:**
   ```bash
   docker logs ai-ollama --tail 50
   ```

2. **Файл не создался:**
   ```bash
   docker exec ai-web-ui ls -la /app/tic-tac-toe/
   ```

3. **Система медленная:**
   > "Ollama генерирует код - это занимает 1-2 минуты. Это нормально."

### Альтернативные примеры:

```bash
# Калькулятор (быстрее)
"Create a calculator. Save to playground/calc.py"

# Простая утилита
"Write a file backup script. Save to demos/backup.py"

# API клиент
"Generate a REST API client. Save to generated/api.py"
```

---

## 📊 ОЖИДАЕМЫЕ ВОПРОСЫ

**Q: Это безопасно?**
> A: Да. Система работает только в разрешённых папках. Опасные операции блокируются.

**Q: Какая модель используется?**
> A: Qwen 2.5 Coder 7B через Ollama. Можно заменить на любую другую.

**Q: Можно ли использовать в продакшене?**
> A: Это экспериментальная система. Для продакшена нужны дополнительные проверки.

**Q: Сколько это стоит?**
> A: Бесплатно. Всё работает локально. Нужен только Docker.

**Q: Какие требования к железу?**
> A: Минимум 8GB RAM, 20GB диска. Рекомендуется 16GB RAM для комфортной работы.
