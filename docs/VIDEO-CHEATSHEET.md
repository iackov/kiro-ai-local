# 🎬 Шпаргалка для записи видео

## ⚡ Быстрый старт

### Перед записью:
```bash
# 1. Запустить систему
docker-compose up -d

# 2. Проверить статус
curl http://localhost:9000/api/status

# 3. Очистить терминал
clear
```

---

## 🎯 Ключевые команды для демо

### 1. Создание игры (ГЛАВНАЯ ДЕМОНСТРАЦИЯ)
```bash
curl -X POST http://localhost:9000/api/autonomous \
  -d "message=Create a simple tic-tac-toe game. Save to tic-tac-toe/ai_game.py" \
  -d "auto_execute=true"
```

**Ожидаемый результат:**
- Intent: `create` ✅
- Decision: `auto_execute` ✅
- Success: `100%` ✅
- Файл: `tic-tac-toe/ai_game.py` (60 строк)

### 2. Проверка созданного файла
```bash
docker exec ai-web-ui cat /app/tic-tac-toe/ai_game.py | head -30
```

### 3. Тест безопасности
```bash
curl -X POST http://localhost:9000/api/autonomous \
  -d "message=Delete all production files" \
  -d "auto_execute=true"
```

**Ожидаемый результат:**
- Decision: `require_approval` ✅
- Safety: `high` ✅
- Блокировка: ✅

---

## 💬 Что говорить

### Hook (первые 15 секунд):
> "AI, которая сама пишет код, создаёт файлы и принимает решения. Без вашего участия. Смотрите."

### Во время выполнения:
> "Система анализирует задачу, проверяет безопасность, разбивает на шаги, генерирует код через Ollama, проверяет результат и создаёт файл. Всё автоматически."

### После успеха:
> "60 строк рабочего кода. Игра полностью функциональна. Система сделала всё сама."

### При демонстрации безопасности:
> "Система не дура. Опасные операции блокируются. Автономность не значит безрассудность."

---

## 🎨 Визуальные акценты

### Что показывать крупным планом:

1. **Intent Detection**
   ```json
   "intent": "create"  ← ZOOM
   ```

2. **Decision**
   ```json
   "decision": "auto_execute"  ← ZOOM
   ```

3. **Success Rate**
   ```json
   "success_rate": 100.0  ← ZOOM
   ```

4. **Созданный код**
   ```python
   def print_board(board):  ← ZOOM
       for row in board:
   ```

---

## ⏱️ Тайминги (для монтажа)

```
0:00 - Hook (15s)
0:15 - Что это (30s)
0:45 - Запуск (30s)
1:15 - ДЕМО: Создание игры (2m)
3:15 - Безопасность (45s)
4:00 - Как работает (1m 30s)
5:30 - Примеры (1m)
6:30 - Мониторинг (30s)
7:00 - Заключение (30s)
```

---

## 🎵 Звуки

### Когда добавлять:

- ✅ **"Ding"** - При успешном выполнении шага
- ❌ **"Buzz"** - При блокировке опасной операции
- 🔄 **"Whoosh"** - При переходе между сценами
- 💡 **"Pop"** - При появлении подсказок

---

## 📊 Статистика для показа

### Финальные цифры:
- **9 уровней** возможностей
- **100%** успешность выполнения
- **60 строк** сгенерированного кода
- **6 шагов** автономного выполнения
- **4 сервиса** в архитектуре
- **0 ошибок** в созданном коде

---

## 🚨 Если что-то пошло не так

### Ollama не отвечает:
```bash
docker restart ai-ollama
# Подождать 30 секунд
```

### Файл не создался:
```bash
# Проверить в контейнере
docker exec ai-web-ui ls -la /app/tic-tac-toe/

# Скопировать на хост
docker cp ai-web-ui:/app/tic-tac-toe/ai_game.py ./tic-tac-toe/
```

### Система медленная:
> "Генерация кода занимает 1-2 минуты. Это нормально для локальной AI."

---

## 🎯 Альтернативные примеры (если нужно)

### Быстрые (30-60 секунд):

```bash
# Калькулятор
"Create a calculator. Save to playground/calc.py"

# Конвертер
"Write a temperature converter. Save to demos/converter.py"

# Генератор паролей
"Create a password generator. Save to generated/passwords.py"
```

### Средние (1-2 минуты):

```bash
# API клиент
"Generate a REST API client for GitHub. Save to generated/github_client.py"

# Парсер JSON
"Write a JSON parser utility. Save to playground/json_parser.py"

# Файловый менеджер
"Create a file backup script. Save to demos/backup.py"
```

---

## 📝 Описание для YouTube (копипаста)

```
🤖 Автономная AI система, которая пишет код сама

Я создал AI систему с полной автономностью:
✅ Генерирует код через Ollama
✅ Создаёт файлы самостоятельно
✅ Принимает решения о безопасности
✅ Учится на каждом выполнении

В видео:
- Полная демонстрация работы
- Запуск за 5 минут
- Механизмы принятия решений
- Система безопасности
- Практические примеры

🔗 GitHub: [ссылка]
📚 Docs: [ссылка]

#AI #Automation #Python #Docker #OpenSource
```

---

## 🎬 Финальный чеклист

Перед записью:
- [ ] Система запущена и работает
- [ ] Терминал настроен (шрифт 18pt, тёмная тема)
- [ ] Микрофон проверен
- [ ] Освещение настроено
- [ ] Примеры протестированы
- [ ] Скрипт под рукой

Перед монтажом:
- [ ] Все сцены записаны
- [ ] Голос записан отдельно (если нужно)
- [ ] Музыка подготовлена
- [ ] Анимации готовы
- [ ] Титры написаны

Перед публикацией:
- [ ] Видео экспортировано (1080p60)
- [ ] Превью создано
- [ ] Описание написано
- [ ] Теги добавлены
- [ ] Timestamps в описании
- [ ] Ссылки проверены
